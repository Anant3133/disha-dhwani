// routes/mentee.js
const express = require('express');
const router = express.Router();
// const menteeController = require('../controllers/menteeController'); // Will uncomment later
// const { verifyToken, isMentee } = require('../middleware/auth'); // Will uncomment later

// Example protected route for mentees
// router.get('/my-learning', verifyToken, isMentee, menteeController.getMenteeDashboard);

module.exports = router;